# Project Rules

## Git Workflow Guidelines
- **Automatic Commit & Push**: After completing code changes, debugging, or feature additions and verifying that they build and function correctly, always stage the changed files (`git add`), commit them with a clear, descriptive commit message (`git commit -m "..."`), and push the commits to the remote repository (`git push`).
